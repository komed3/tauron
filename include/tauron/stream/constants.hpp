#pragma once

#include <cstddef>

namespace tauron::stream {

inline constexpr std::size_t MIN_SEQ_BLOCKS = 32;
inline constexpr std::size_t MAX_SEQ_BLOCKS = 255;

inline constexpr std::size_t PAYLOAD_SIZE = 28;

}
